{
  "batch": "BL-IMG-PERSIST-GCS",
  "created_at": "2026-06-01",
  "features": [
    {
      "id": "F-IGP-01",
      "title": "GCS 存储抽象层 + 配置（@google-cloud/storage + ADC + feature flag）",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "source": "BL-IMG-PERSIST-GCS Spec F-IGP-01；docs/audits/image-download-failure-diagnosis-2026-06-01.md 根治方案",
      "acceptance": "1) 新增 src/lib/storage/image-store.ts(接口) + gcs-image-store.ts(GCS 实现)：putImage({key,body,contentType}) + getImageObject(key)；2) ADC 认证（GCP VM 默认 SA），env GCS_IMAGE_BUCKET(启用时必填) + IMAGE_PERSIST_ENABLED(默认 true)；3) put/get round-trip 可用（dev 桶或 mock）；4) 缺 bucket 时按 D6 兜底不 crash；5) .env.example 增两项 + 注释；6) npx tsc --noEmit PASS；7) npm run build PASS；8) 独立 commit feat(BL-IMG-PERSIST-GCS F-IGP-01)"
    },
    {
      "id": "F-IGP-02",
      "title": "持久化 helper（url-http/url-data:/b64_json 三形态归一）+ 同步接入两入口 + 落库 keys",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "source": "BL-IMG-PERSIST-GCS Spec F-IGP-02；D4 同步时序 + D5 三形态 + types.ts:188-189 b64_json 被忽略缺陷",
      "acceptance": "1) src/lib/api/persist-image.ts: persistGeneratedImages(traceId,projectId,response) 处理 url-http(fetch)/url-data:(解码)/b64_json(解码) 三形态 → 上传 GCS(key images/{projectId}/{traceId}/{idx}.{ext}) → 返回 keys(失败项 null)；2) 同步 await 接入 generations/route.ts + mcp/tools/generate-image.ts，构造响应前完成；3) original_urls 改存 GCS keys，且在对外代理 URL 可用前已可解析（消除 fire-and-forget 竞态）；4) 三形态 fixture 均成功上传返回 key；5) original_urls = GCS keys（非上游 URL 非 metadata）；6) 竞态闭合：响应返回后立即 GET 代理 URL 可解析；7) 上传失败按 D6 兜底生成不硬失败；8) tsc + build PASS；9) 独立 commit feat(BL-IMG-PERSIST-GCS F-IGP-02)"
    },
    {
      "id": "F-IGP-03",
      "title": "代理回源改读 GCS + 签名 TTL 90d + MCP/API 统一返回可用代理 URL",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "source": "BL-IMG-PERSIST-GCS Spec F-IGP-03；MCP data: 死链 + b64_json 空数组 双缺陷修复",
      "acceptance": "1) proxy/[traceId]/[idx]/route.ts: original_urls[idx] 作为 GCS key → getImageObject 流式回传，去 fetch 上游分支，保留 sanitizeImageContentType + nosniff，对象缺失 404；2) image-proxy.ts DEFAULT_TTL_SECONDS → 90d；3) MCP generate-image.ts 对每张持久化图(含 data:/b64_json)构造代理 URL；4) API generations/route.ts 所有图 data[i].url 置可用代理 URL，b64_json 字段原样透传不删(D7)；5) 经 MCP gpt-image-mini/gemini-3-pro-image 生成 → 代理 URL GET 200 返回图(修复前必 404)；6) seedream-3 同样 200；7) b64_json-only fixture → MCP images[] 非空；8) tsc + build PASS；9) 独立 commit feat(BL-IMG-PERSIST-GCS F-IGP-03)"
    },
    {
      "id": "F-IGP-04",
      "title": "日志详情页可回看持久化图 + 下载（server 端新签代理 URL 渲染 <img>）",
      "priority": "medium",
      "executor": "generator",
      "status": "done",
      "source": "BL-IMG-PERSIST-GCS Spec F-IGP-04；F-ILDF-01 strip 导致日志不可回看",
      "acceptance": "1) logs/[traceId]/page.tsx: CallLog 含持久化图资产时对每张图用 buildProxyUrl(traceId,idx,origin) 渲染 <img>，不再显示 [image:fmt,NKB]；非图文本保留原分支；2) responseContent/summarizeImageUrl 用法调整使日志页能渲染（复用 isImageUrl 已识别 /v1/images/proxy/）；3) 持久化生成后日志详情页显示图而非 metadata；4) 图可右键另存/直接打开（同源代理 90d 内有效）；5) n>1 多图全渲染；6) 中英 i18n 文案正常；7) tsc + build PASS；8) 独立 commit feat(BL-IMG-PERSIST-GCS F-IGP-04)"
    },
    {
      "id": "F-IGP-05",
      "title": "Codex 验收 + 签收报告",
      "priority": "high",
      "executor": "codex",
      "status": "pending",
      "source": "BL-IMG-PERSIST-GCS Spec 验收",
      "acceptance": "1) scripts/test/codex-setup.sh + wait PASS；2) 复现修复：MCP base64 模型生成图 → 代理 URL GET 200(对照修复前 404)，b64_json-only fixture 非空；3) 回归既有图片测试(image-proxy/summarize-image-url/process-image-base64-strip/is-image-url)——original_urls 语义改 GCS key + data: 不再内联，相关用例随之更新(Codex 测试域)；4) 存储层 put/get(mock 或 dev 桶)；5) D6 兜底路径(存储故障不硬失败)验证；6) 日志页可回看图(视觉/DOM)；7) npx tsc --noEmit / npm run build / npm run test PASS；8) 前置 ops 清单(gcloud 命令 + lifecycle json)齐备可执行；9) 输出 docs/test-reports/BL-IMG-PERSIST-GCS-signoff-YYYY-MM-DD.md 含命令证据 + 结论 PASS/FAIL"
    }
  ]
}
