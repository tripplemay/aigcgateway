{
  "batch": "BL-RECON-FIX-PHASE1",
  "created_at": "2026-04-27",
  "features": [
    {
      "id": "F-RF-01",
      "title": "Volcengine fetcher 加 ExpenseDate 过滤（消除月度账单重复入对账表）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "source": "BL-RECON-FIX-PHASE1 spec § F-RF-01",
      "acceptance": "1) src/lib/billing-audit/fetchers/volcengine.ts fetchDailyBill 返回前过滤 ExpenseDate ≠ requestedDate 的行（YYYY-MM-DD UTC 比较）；缺失 ExpenseDate 字段保守过滤；2) 单测覆盖 3 case：mock 月度全量 3 条 ExpenseDate（04-02/04-15/04-23）→ fetchDailyBill(04-23) 仅返 1 条；mock 全部 ExpenseDate≠target → 返空数组；mock 缺失 ExpenseDate → 过滤；3) tsc + build + vitest 全过；4) commit 'feat(BL-RECON-FIX-PHASE1 F-RF-01): volcengine fetcher ExpenseDate filter'"
    },
    {
      "id": "F-RF-02",
      "title": "reconcile-job tier1 path 加 CNY→USD 货币转换",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "source": "BL-RECON-FIX-PHASE1 spec § F-RF-02",
      "acceptance": "1) src/lib/billing-audit/reconcile-job.ts tier1 path bill loop 内：const exchangeRate = await getConfigNumber('EXCHANGE_RATE_CNY_TO_USD', 0.137)；const upstreamUsd = bill.currency === 'CNY' ? bill.amount * exchangeRate : bill.amount；upsertReconciliation 写 upstreamAmount=upstreamUsd + details.currency=bill.currency + details.upstreamAmountOriginal=bill.amount + details.exchangeRateApplied=（CNY 时 exchangeRate 否则 1）；2) 单测扩展：USD 不变 / CNY amount=3.25 → 0.44525 / 自定义 EXCHANGE_RATE_CNY_TO_USD=0.14 时按新汇率；3) tier 2 balance path 不动；4) tsc + build + vitest 全过；5) commit 'feat(BL-RECON-FIX-PHASE1 F-RF-02): tier1 CNY-to-USD currency conversion'"
    },
    {
      "id": "F-RF-03",
      "title": "Image pricing audit 脚本 + 生产数据报告（read-only，不改 DB）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "source": "BL-RECON-FIX-PHASE1 spec § F-RF-03",
      "acceptance": "1) 新建 scripts/audit-image-pricing.ts：查所有 modality=image 的 model + 关联 channel（含 costPrice + sellPrice + provider name）+ 30 天 call_logs sum(costPrice)/count(*)/avg；输出 markdown 表格按模型分块；2) 标注 ⚠️ 可疑 = modality=image AND channel.costPrice.unit=='token'（image 模型按 token 计费，与 openrouter 上游 perCall 计费规则不一致，可能漏算）；3) 脚本 read-only（无 INSERT/UPDATE/DELETE）；4) 用 prod-readonly DATABASE_URL 跑出 docs/audits/image-pricing-2026-04-27.md；5) 报告 commit 入仓；6) tsc 编译通过；7) commit 'feat(BL-RECON-FIX-PHASE1 F-RF-03): image pricing audit script + 2026-04-27 prod report'"
    },
    {
      "id": "F-RF-04",
      "title": "BL-RECON-FIX-PHASE1 全量验收（fetcher 行为 + 货币转换 + audit 脚本 + 静态）",
      "priority": "high",
      "executor": "codex",
      "status": "pending",
      "source": "BL-RECON-FIX-PHASE1 spec § F-RF-04",
      "acceptance": "静态（3）：tsc / build / vitest（≥ 445 + F-RF-01/02 新增）。Volcengine fetcher 行为（2）：4) F-RF-01 单测全 PASS；5) 集成模拟：mock fetcher 返月度 3 条 ExpenseDate → reconcile cross 5 天 reportDate 仅 ExpenseDate=对应日的 1 行 BillReconciliation。CNY→USD（2）：6) F-RF-02 单测全 PASS；7) mock {currency:'CNY', amount:3.25} → upstreamAmount=0.44525 + details.upstreamAmountOriginal=3.25 + details.currency='CNY' + details.exchangeRateApplied=0.137。Audit 脚本（2）：8) npx tsx scripts/audit-image-pricing.ts 在测试 DB 跑通无错（即使无数据输出空表）；9) mock 一个 image modality + token-priced channel 跑脚本验证 ⚠️ 标记输出。报告（1）：10) docs/test-reports/BL-RECON-FIX-PHASE1-signoff-2026-04-2X.md 含 9 项证据 + 关键截图（audit 报告输出片段）。明确排除：不要求生产数据 reconcile 重跑 / 不要求历史 BIG_DIFF 回填 / 不要求修改任何 channel 配置（Phase 2）"
    }
  ]
}
