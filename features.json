{
  "batch": "BL-SEC-BILLING-CHECK-FOLLOWUP",
  "created_at": "2026-04-18",
  "features": [
    {
      "id": "F-BCF-01",
      "title": "修正 CHECK migration（v2）+ schema.prisma 注释",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "source": "BL-SEC-BILLING-AI F-BA-03 deferred",
      "acceptance": "1) 新建 `prisma/migrations/20260418_billing_check_constraints_v2/migration.sql`（v2 命名与失败的 v1 区分）；2) DO $$ 预检用**正确规则**：transactions 违规 = (type='DEDUCTION' AND amount>=0) OR (type IN ('REFUND','RECHARGE','BONUS') AND amount<0)，template_ratings 违规 = score<1 OR score>5；3) ALTER TABLE transactions ADD CONSTRAINT transactions_amount_sign_check CHECK ((type='DEDUCTION' AND amount<0) OR (type IN ('REFUND','RECHARGE','BONUS') AND amount>=0) OR (type='ADJUSTMENT'))；4) ALTER TABLE template_ratings ADD CONSTRAINT template_ratings_score_range_check CHECK (score>=1 AND score<=5)；5) schema.prisma 在 model Transaction.amount 和 model TemplateRating.score 字段上方加 `/// CHECK: ...` + `/// 在 migration 20260418_billing_check_constraints_v2 定义` 文档注释（Prisma 不支持 @@check 属性）；6) 本地 `npx prisma migrate dev` 跑通，无冲突；7) tsc + build 通过"
    },
    {
      "id": "F-BCF-02",
      "title": "BL-SEC-BILLING-CHECK-FOLLOWUP 生产部署 + 验收",
      "priority": "high",
      "executor": "codex",
      "status": "pending",
      "source": "BL-SEC-BILLING-CHECK-FOLLOWUP",
      "acceptance": "构建健全（本地）：1) npx prisma migrate dev v2 migration 跑通；2) npm run build 通过；3) npx tsc --noEmit 通过。生产数据预检：4) SELECT COUNT(*) FROM transactions WHERE (type='DEDUCTION' AND amount>=0) OR (type IN ('REFUND','RECHARGE','BONUS') AND amount<0) = 0；5) SELECT COUNT(*) FROM template_ratings WHERE score<1 OR score>5 = 0。生产部署：6) ssh 生产 git pull + prisma migrate deploy 成功；7) \\d+ transactions 见到 transactions_amount_sign_check；8) \\d+ template_ratings 见到 template_ratings_score_range_check。CHECK 功能验证（生产 psql）：9) INSERT transactions(type='DEDUCTION',amount=10) → 23514；10) INSERT transactions(type='REFUND',amount=-10) → 23514；11) INSERT template_ratings(score=0) → 23514；12) INSERT template_ratings(score=6) → 23514。正向回归：13) 生产发一次 AI 调用触发 DEDUCTION 正常完成（查最新 call_log + transaction 一对对齐）。14) 生成 signoff 报告 docs/test-reports/BL-SEC-BILLING-CHECK-FOLLOWUP-signoff-2026-04-18.md。"
    }
  ]
}
