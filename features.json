{
  "batch": "BL-SEC-CRED-HARDEN",
  "created_at": "2026-04-18",
  "features": [
    {
      "id": "F-CH-01",
      "title": "image-proxy.ts 移除 HMAC fallback + 启动校验 env",
      "priority": "critical",
      "executor": "generator",
      "status": "completed",
      "source": "CRIT-6",
      "acceptance": "1) `src/lib/api/image-proxy.ts` 删除 `?? 'aigc-gateway-image-proxy-dev-secret'` 字符串 fallback；2) getSecret() 改为若 IMAGE_PROXY_SECRET || AUTH_SECRET || NEXTAUTH_SECRET 均为空则 throw Error（含明确变量名提示）；3) 在 `src/lib/env.ts`（或新增 assertEnv 函数）加启动校验，应用启动阶段即抛错而非首次请求；4) 新增单测 `src/lib/api/__tests__/image-proxy.test.ts`：env 缺失时 throw + 三级 fallback 任一有值时通过；5) tsc 通过；6) `npm run build` 通过；7) git grep 'aigc-gateway-image-proxy-dev-secret' 零结果（除 docs/）"
    },
    {
      "id": "F-CH-02",
      "title": "prisma/seed.ts admin 密码改读 ADMIN_SEED_PASSWORD",
      "priority": "critical",
      "executor": "generator",
      "status": "completed",
      "source": "CRIT-5 (part 1)",
      "acceptance": "1) `prisma/seed.ts:284` 删除 `hashSync('admin123', 12)` 字面量；2) 改为 `hashSync(requireEnv('ADMIN_SEED_PASSWORD'), 12)`，requireEnv helper 可放在 seed.ts 顶部或 `prisma/lib/require-env.ts`；3) 保留 seed 幂等性：upsert update 块不含 passwordHash（已存在 admin 不重置密码）；4) `.env.example` 新增 `ADMIN_SEED_PASSWORD=` 占位；5) `.env.production` 不设此变量（防止 prod 跑 seed）；6) `npx tsx prisma/seed.ts` 本地跑通（需本地 export ADMIN_SEED_PASSWORD=...）；7) git grep 'admin123' 零结果（除 docs/ 引用）"
    },
    {
      "id": "F-CH-03",
      "title": "scripts + test scripts 密码 env 化（Codex@2026! + Test1234）",
      "priority": "critical",
      "executor": "generator",
      "status": "completed",
      "source": "CRIT-5 (part 2) + L5",
      "acceptance": "1) 新建 `scripts/lib/require-env.ts` 导出 `requireEnv(key): string` —— 缺失时 console.error + process.exit(1)；2) 以下文件密码改 `requireEnv('ADMIN_TEST_PASSWORD')`：`scripts/admin-auth.ts`、`scripts/stress-test.ts`、`scripts/test/template-governance-eval.mjs`、`scripts/test/bf-fork-project-switch-verifying-e2e-2026-04-10.ts`、`scripts/test/rate-limit-f-rl-08-verifying-e2e-2026-04-15.ts`、其余用 `git grep -l 'Codex@2026\\\\!'` 枚举的所有命中文件；3) 含 'Test1234' 的 E2E 脚本改 `requireEnv('E2E_TEST_PASSWORD')`，用 `git grep -l 'Test1234'` 枚举；4) `.env.example` 新增 `ADMIN_TEST_PASSWORD=`、`E2E_TEST_PASSWORD=` 占位；5) 本地跑 `npx tsx scripts/admin-auth.ts`（无 env）应 exit(1) 打 'Missing env: ADMIN_TEST_PASSWORD'；6) git grep 'Codex@2026' + 'Test1234' 零结果（除 docs/）；7) tsc 通过；8) CI `.github/workflows/` 检查 secrets 文档化需要注入的 env（作为 spec 附注，不改 yml）"
    },
    {
      "id": "F-CH-04",
      "title": "BL-SEC-CRED-HARDEN 全量验收",
      "priority": "critical",
      "executor": "codex",
      "status": "completed",
      "source": "BL-SEC-CRED-HARDEN",
      "acceptance": "静态检查：1) git grep -i 'admin123' 零结果；2) git grep 'Codex@2026' 零结果；3) git grep 'Test1234' 零结果；4) git grep 'aigc-gateway-image-proxy-dev-secret' 零结果（以上 4 条均允许 docs/ 目录引用但代码/scripts/prisma 需清零）；5) `.env.example` 含 4 个新变量（ADMIN_SEED_PASSWORD + ADMIN_TEST_PASSWORD + E2E_TEST_PASSWORD + IMAGE_PROXY_SECRET）。代码健全：6) `npm run build` 通过；7) `npx tsc --noEmit` 通过；8) `npx vitest run src/lib/api/__tests__/image-proxy.test.ts` 新单测通过。启动校验：9) 空 IMAGE_PROXY_SECRET + 空 AUTH_SECRET + 空 NEXTAUTH_SECRET 启动 `npm run dev` 应抛错退出（或首次请求 image-proxy 时抛错，二选一）。脚本健全：10) `ADMIN_TEST_PASSWORD= npx tsx scripts/admin-auth.ts` exit(1)；11) 设置 env 后 admin-auth.ts 能正常获取 token。12) 生成 signoff 报告 `docs/test-reports/BL-SEC-CRED-HARDEN-signoff-2026-04-18.md`。"
    }
  ]
}
