{
  "batch": "REGRESSION-BACKFILL",
  "created_at": "2026-04-15",
  "features": [
    {
      "id": "F-RB-01",
      "title": "BILLING-REFACTOR 回溯 regression tests",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "completed_at": "2026-04-15T19:20:00Z",
      "source": "BL-120",
      "acceptance": "1) scripts/test-mcp.ts 增加 'billing: list_models pricing = deducted amount'：调用 chat 后读取 get_log_detail 的 cost，对比 list_models 对应 alias 的 pricing 计算值（prompt×input_per_1m + completion×output_per_1m），误差 < $0.00000001；2) scripts/test-mcp.ts 增加 'billing: image generate cost = alias perCall'：调用 generate_image 后验证 cost = alias.sellPrice.perCall；3) scripts/test-mcp.ts 增加 'billing: pricing has no float noise'：断言所有 list_models 返回的 pricing 数值小数位 ≤ 6；4) 每条测试用 // BL-120 / BILLING-REFACTOR 注释标注来源；5) tsc 通过"
    },
    {
      "id": "F-RB-02",
      "title": "AUDIT-SEC 回溯 regression tests",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "completed_at": "2026-04-15T19:20:00Z",
      "note": "disabled-model 断言用 deprecated flag 代替（不修 DB 状态），其余 3 条全覆盖",
      "source": "BL-120",
      "acceptance": "1) scripts/test-mcp.ts 增加 'audit-sec: list_models filters disabled models'：使用 admin API 或 DB 直接禁用一个 model 后调用 list_models，断言该 model 不在返回列表（测试完恢复）；2) scripts/test-mcp.ts 增加 'audit-sec: image models have supportedSizes'：list_models(modality='image') 每个返回项都有 supportedSizes 非空数组；3) scripts/test-mcp-errors.ts 增加 'audit-sec: generate_image invalid size returns supportedSizes list'：传 9999x9999，断言 error 含 invalid_size 且包含 supportedSizes 字段；4) scripts/test-mcp.ts 增加 'audit-sec: free_only filter returns zero-priced aliases'：list_models(free_only=true) 返回的所有 alias 都应 pricing 全为 0；5) 每条测试注释 // BL-120 / AUDIT-SEC 来源；6) tsc 通过"
    },
    {
      "id": "F-RB-03",
      "title": "DX-POLISH 回溯 regression tests",
      "priority": "high",
      "executor": "generator",
      "status": "done",
      "completed_at": "2026-04-15T19:20:00Z",
      "note": "reasoning_tokens 已由 F-AF-02 回归覆盖（test-mcp.ts 16f），本批次不重复",
      "source": "BL-120",
      "acceptance": "1) scripts/test-mcp.ts 增加 'dx-polish: chat rejects image modality model'：chat(model='gpt-image-mini') 返回 invalid_model_modality；2) scripts/test-mcp.ts 增加 'dx-polish: capability=vision only returns text models'：list_models(capability='vision') 所有 modality='text'；3) scripts/test-mcp.ts 增加 'dx-polish: reasoning_tokens present in get_log_detail'：调用 reasoning 模型后，get_log_detail.usage.reasoningTokens 存在且 > 0；4) scripts/test-mcp.ts 增加 'dx-polish: json_mode strips markdown code fence'：chat(response_format={type:'json_object'}) 的 content JSON.parse 成功，不含 markdown 围栏；5) scripts/test-mcp-errors.ts 增加 'dx-polish: get_action_detail not-found wording includes in this project'；6) 每条测试注释 // BL-120 / DX-POLISH 来源；7) tsc 通过"
    },
    {
      "id": "F-RB-04",
      "title": "邮箱验证高风险路径 regression",
      "priority": "medium",
      "executor": "generator",
      "status": "pending",
      "source": "BL-073（部分）",
      "acceptance": "1) scripts/e2e-test.ts 增加 'email-verify: register → unverified → verify token → login OK' 全链路步骤，不依赖真实发邮件（直接从 DB 读 emailVerificationToken）；2) scripts/e2e-errors.ts 增加 'email-verify: invalid token rejected'；3) scripts/e2e-errors.ts 增加 'email-verify: expired token rejected'（构造一个过期的 token row）；4) scripts/e2e-errors.ts 增加 'email-verify: already-verified repeat call 幂等'；5) 断言 register 后 user.emailVerified=false；6) 每条测试注释 // BL-073 来源；7) tsc 通过"
    },
    {
      "id": "F-RB-05",
      "title": "scripts 运行基础设施升级（test-all 统一入口）",
      "priority": "medium",
      "executor": "generator",
      "status": "done",
      "completed_at": "2026-04-15T19:00:00Z",
      "note": "4 个 scripts/*.ts 的 BASE_URL/API_KEY 约定和结尾 pass/fail 打印+exit code 已经一致，无需改动。test-all.sh 新增一键入口，支持 BASE_URL/API_KEY/ZERO_BALANCE_API_KEY 透传，逐个执行并汇总。bash -n 语法通过。本地实跑留给 Reviewer 在 F-RB-06 验证。",
      "acceptance": "1) 所有 scripts/test-*.ts 从 process.env 读取 BASE_URL / API_KEY 的方式统一（已经统一则保持）；2) 新增 scripts/test-all.sh 一键跑 4 个脚本（e2e-test / e2e-errors / test-mcp / test-mcp-errors）并汇总 pass/fail；3) 每个脚本末尾打印最终 'passed=X failed=Y'，exit code 正确（失败返回 1）；4) test-all.sh 能通过环境变量覆盖 BASE_URL / API_KEY；5) tsc 通过；6) 本地 L1 环境实际跑一次验证全绿"
    },
    {
      "id": "F-RB-06",
      "title": "REGRESSION-BACKFILL 全量验收",
      "priority": "high",
      "executor": "codex",
      "status": "pending",
      "acceptance": "codex 执行：1) 对 4 个 scripts/*.ts 做代码审阅，确认本批次新增的所有测试点都存在且断言正确（含来源注释）；2) 启动 dev server（BASE_URL=http://localhost:3099），在 L1 环境跑 scripts/test-all.sh，全绿通过；3) 至少抽 1 条新断言做 mutation 验证：把产品代码对应修复暂时回退一行，验证该断言能失败（证明测试不是空壳）；4) 不破坏现有 scripts/*.ts 原有测试步骤；5) 签收报告生成"
    }
  ]
}
