## 1. 生成性能测试计划

`使用 $aigc-gateway-performance-testing，基于 docs/AIGC-Gateway-Full-PRD.md，为当前项目生成一版完整性能测试计划。 要求： - 输出场景矩阵 - 按公开接口、普通用户、管理员、后台任务分组 - 标出每个场景的目标指标、风险点和执行顺序`

## 2. 只做重点接口压测设计

`使用 $aigc-gateway-performance-testing，为以下接口设计性能测试方案： - GET /api/v1/models - POST /api/auth/login - GET /api/admin/sync-status 要求： - 给出每个接口的 smoke / load / stress 分层方案 - 给出建议并发、请求量、通过标准 - 暂不执行`

## 3. 本地测试环境执行一轮轻量压测

`使用 $aigc-gateway-performance-testing，在本地测试环境执行一轮低风险性能测试。 范围： - GET /api/v1/models - POST /api/auth/login 要求： - 先做 smoke - 再做小流量并发测试 - 输出 p50 / p95 / p99、吞吐、成功率和结论`

## 4. 为某个接口生成 k6 脚本

`使用 $aigc-gateway-performance-testing，为 POST /api/admin/sync-models 生成一份 k6 压测脚本草案。 要求： - 包含阈值配置 - 包含阶段式升压 - 说明适合在什么环境执行`

## 5. 为某个接口生成 autocannon 执行方案

`使用 $aigc-gateway-performance-testing，为 GET /api/v1/models 生成一套 autocannon 回归压测命令。 要求： - 分成 smoke、标准负载、压力上探三档 - 每档都给出推荐命令和风险说明`

## 6. 做一次生产环境低风险性能验证

`使用 $aigc-gateway-performance-testing，在生产环境做一轮低风险性能验证。 范围： - GET /api/v1/models - GET /api/admin/sync-status 要求： - 严格控制请求量 - 不做高副作用写操作 - 输出指标和风险结论`

## 7. 分析一次性能问题

`使用 $aigc-gateway-performance-testing，帮我分析当前系统的性能风险。 背景： - 管理员手动同步偶发超时 - /api/v1/models 响应有时波动较大 要求： - 基于 PRD 和现有架构判断可能瓶颈 - 给出验证顺序 - 给出最小成本的排查方案`

## 8. 生成正式性能测试报告

`使用 $aigc-gateway-performance-testing，基于已执行的测试结果整理一份正式性能测试报告。 要求： - 包含测试目标、环境、场景、指标、通过项、失败项、风险项、结论 - 输出适合存档的 markdown`

如果你想更顺手，平时可以记一个最通用母版：

`使用 $aigc-gateway-performance-testing，针对【环境】里的【接口/链路】执行【方案设计 / 脚本生成 / 实际压测 / 报告整理】。 要求： - 【限制条件】 - 【指标要求】 - 【输出形式】`

比如你以后只要替换这几个槽位：