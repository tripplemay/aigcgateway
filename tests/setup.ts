/**
 * Vitest global setup — loaded by setupFiles in vitest.config.ts.
 *
 * F-TI-01: minimal placeholder. F-TI-02 expands with MSW lifecycle
 * (server.listen / resetHandlers / close) + jest-dom matchers.
 */
import "@testing-library/jest-dom/vitest";
