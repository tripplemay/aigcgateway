# Settings — Design Notes

## Ignore (fabricated)
- "System Status" display (API Latency, region status) — no user-facing system status API

## Ignore (no backend support)
- Notification preferences toggle — no preferences API

## Fully supported
- Display/edit name, change password, login history, sign out (client-side JWT discard)
