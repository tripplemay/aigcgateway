# Keys List — Design Notes

## Ignore (no backend support)
- "Daily Capacity 1,000,000 req" stat card — no daily quota concept
- Global search bar — no cross-resource search endpoint

## Partial support
- Enable/Disable toggle — API only supports permanent revocation (REVOKED), no re-enable. Use revoke button instead of toggle.
